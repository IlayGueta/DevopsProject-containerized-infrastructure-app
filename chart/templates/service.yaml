apiVersion: v1
kind: Service

metadata:
  name: {{ include "infrastructure-api.fullname" . }}

spec:
  type: {{ .Values.service.type }}

  selector:
    app: {{ include "infrastructure-api.fullname" . }}

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}